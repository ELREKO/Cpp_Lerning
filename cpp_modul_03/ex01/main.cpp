#include "src/ScavTrap.hpp"

int main()
{
	ScavTrap scav1;
	// ScavTrap scav2("Max");
	// ScavTrap scav3(scav2);

	// std::cout << std::endl << "---------------------------------" << std::endl;
	// scav1.attack("Max");
	// scav2.takeDamage(20);

	// std::cout << std::endl << "---------------------------------" << std::endl;
	// scav1.attack("Max");
	// scav2.takeDamage(20);
	// scav2.beRepaired(5);

	// std::cout << std::endl << "---------------------------------" << std::endl;
	// scav2.attack("no Name");
	// scav1.takeDamage(100);

	// std::cout << std::endl << "---------------------------------" << std::endl;
	// scav2.attack("no name");
	// scav1.takeDamage(5);
	// scav1.beRepaired(10); 

    // std::cout << std::endl << "---------------------------------" << std::endl;
	// scav1 = scav2; 

	// std::cout << std::endl << "---------------------------------" << std::endl;
	// scav1.guardGate();
}