#include "src/FragTrap.hpp"

int main()
{
	// FragTrap frag1;
	FragTrap frag2("Max");
	// FragTrap frag3(frag2);

	// std::cout << std::endl << "---------------------------------" << std::endl;
	// frag1.attack("Max");
	// frag2.takeDamage(20);

	// std::cout << std::endl << "---------------------------------" << std::endl;
	// frag1.attack("Max");
	// frag2.takeDamage(20);
	// frag2.beRepaired(5);

	// std::cout << std::endl << "---------------------------------" << std::endl;
	// frag2.attack("no Name");
	// frag1.takeDamage(100);

	// std::cout << std::endl << "---------------------------------" << std::endl;
	// frag2.attack("no name");
	// frag1.takeDamage(5);
	// frag1.beRepaired(10); 

    // std::cout << std::endl << "---------------------------------" << std::endl;
	// frag1 = frag2; 

	// std::cout << std::endl << "---------------------------------" << std::endl;
	// frag1.highFiveGuys();
}